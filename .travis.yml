language: node

services:
  - docker

before_install:
  - docker build -t kalaxia/front . 
  - docker run -it kalaxia/front -v ${TRAVIS_BUILD_DIR}:/srv/app npm test

script:
  - npm test

deploy:
  - provider: script
    script: bash deploy/prod.sh
    on:
      tags: true
  - provider: script
    script: bash deploy/preprod.sh
    on:
      branch: develop
  
after_success:
  - wget https://raw.githubusercontent.com/DiscordHooks/travis-ci-discord-webhook/master/send.sh
  - chmod +x send.sh
  - ./send.sh success $WEBHOOK_URL
after_failure:
  - wget https://raw.githubusercontent.com/DiscordHooks/travis-ci-discord-webhook/master/send.sh
  - chmod +x send.sh
  - ./send.sh failure $WEBHOOK_URL